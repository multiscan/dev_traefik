<%
DOMAIN=ENV['DEV_DOMAIN'] || 'dev.jkldsa.com'
%>
http:
  routers:
    traefik:
      rule: Host("traefik.<%= DOMAIN %>")
      service: api@internal
      tls: {}
    metrics:
      rule: Host("traefik.<%= DOMAIN %>") && PathPrefix("/metrics")
      service: prometheus@internal
      tls: {}